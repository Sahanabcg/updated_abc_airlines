[pytest]
# Tell pytest where tests live and how to find them
testpaths = tests
python_files = test_*.py *_test.py

# Async settings (fixes your warning)
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session

# Optional: quiet output by default, show short tracebacks
addopts = -q
